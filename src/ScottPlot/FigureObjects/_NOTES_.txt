FigureObjects is an experimental namespace.

The goal is to make every part of a figure capable of rendering itself (like plottables).

This will simplify the render loop and make it easier to implement alternative rendering libraries in the future. 